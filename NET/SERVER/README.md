Para buildar favor utilize o comando `make`.
Token atualmente é `token-secreto` que é enviado no header customizado `X-TOKEN-UV`.
Planos futuros:
- Criar uma estrutura segura com criptografia inteligente.
- Implementar um banco de dados local e criar um pequeno CRUD em C.